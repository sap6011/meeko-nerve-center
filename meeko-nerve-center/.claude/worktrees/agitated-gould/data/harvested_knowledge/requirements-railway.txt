# Railway deployment requirements (Linux compatible)
# Note: MLX is macOS-only, so this deployment runs dashboard in monitor mode

# Core (skip MLX - macOS only)
numpy>=1.24.0
safetensors>=0.4.0

# Web/API
aiohttp>=3.9.0
websockets>=12.0

# Dashboard
flask>=3.0.0
flask-socketio>=5.3.0
python-socketio>=5.10.0
eventlet>=0.35.0
gevent>=24.2.1
gevent-websocket>=0.10.1

# Utils
python-dotenv>=1.0.0
