# __PROJECT_NAME__ Project Context

## Overview
__PROJECT_DESCRIPTION__

## Architecture
Roles and their tech stacks are defined in `.ralph/config.json` under the `roles` section.
Each role has its own source directory, instructions, and file extensions.

## Services
| Service | Port | Path | Description |
|---------|------|------|-------------|
| (add your services here) | | | |

## Network
Agent IPs and roles are configured in `.ralph/config.json` under the `agents` section.
Configure each agent's `host` (IP address) and `ssh_alias` there.

## Coding Standards
- (add your project-specific coding rules here)

## Dependencies
- (list common dependencies)
